table_env

{
    "completed": false,
    "description": "Wash spoon"
}


Extract table

Route               Controller              Repository      DB
/extract_table      request.get_image()
                    extract_table(image)
                    hasil_extract : excel

                    return hasil_extract


Issue : gimana cara masukin file excel ke form validasi?

Quest :
Skenario 1 : 
- apakah bisa generate form validasi berdasarkan format excel? asumsikan bisa (jadi form validasi nya kaya ngedit di aplikasi excel aja)
- gimana cara nyimpen dari file excel ke database?

Skenario 2 :
- convert excel ke dataframe
- dari dataframe di kasih rule buat jadi json
- json masukin ke form validasi
- dari form validasi bisa langsung simpen ke database


/save_validasi      request.get_json()      save():
                    save(json)              db.insert_one(json)




# api yang dibutuhin
# 1. extract table brdsarkan jenis dokumen (jenis dokumen, image)
# 2. save hasil validasi ke db
# 3. tambah template baru

"""
routing :
/table
1. POST (one document)
2. GET (all document)

/table/:hash-code
1. GET (one document)
2. PUT (one document)
3. DELETE (one document)

Advance
/jenis-dokumen
1. buat jenis dokumen baru (Post)
2. get all jenis dokumen (get)

/jenis-dokumen/:id-dok
1. edit jenis dokumen (Put)
2. delete jenis dokumen (delete)
"""

"""
db :
1. hasil ekstrak dokumen
2. list jenis dokumen (rule biasa/khusus)
- nama jenis dokumen
- header : list header <brrti semua kolom harus dijadiin header, ngg boleh ada yg d skip + jenis tipe datanya (teks/bil bulat/desimal/list)>
- jumlah gambar input > 1 (true, false)
- jenis tabel (umum/khusus)

spesial buat yg tabel khusus :
- ada aturan tambahan buat list header 
"""
